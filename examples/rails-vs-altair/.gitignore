# Bundler's vendored gem install (104 MB) — never commit.
app/rails/vendor/bundle/
app/rails/.bundle/

# Build artifact, regenerated by scripts/bench.sh.
# Keep the committed results/*.json|*.out from a real run for the README.