# Copy to `.env` and edit — `.env` is gitignored.
# Leave unset to use the defaults in `config/database.yml`.
# DATABASE_URL=postgres://user:pass@localhost/showcase_production
# SECRET_KEY_BASE=<generate with: openssl rand -hex 64>
# PORT=3000
